{
  "name": "tenex-bridge",
  "version": "1.0.0",
  "description": "HTTP bridge service for tenex multi-agent AI platform integration with n8n",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
